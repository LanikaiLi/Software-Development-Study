# hello-goodbye-server
## debugging practice

Build command: npm install
Start command: npm start

Routes:
- `GET /hello` returns `hello 👋`
- `GET /goodbye` returns `goodbye 👋`

This server has at least 5 bugs preventing it from running properly. Try to locate the bugs and connect to the server's two available routes.